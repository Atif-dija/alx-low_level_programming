#include "main.h"
#include<string.h>
#include<stdlib.h>

/**
 * _strlen - function that returns the length of a string
 *
 * @s: input of function
 *
 * Return: the length of a string
 */

int _strlen(char *s)
{
	size_t length;

	length = strlen(s);

	return (length);
}
